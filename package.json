{
  "name": "responsible-wp-frontend",
  "version": "1.0.0",
  "description": "Hybrid site (SSR / SSG) for Responsible IT WordPress frontend",
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "rimraf dist && pnpm exec eleventy --serve",
    "build": "rimraf dist && pnpm exec eleventy",
    "server": "nodemon routing/server.js",
    "server:fresh": "pnpm run build && pnpm run server",
    "dock": "docker rm -f 11ty-app & docker build -t 11ty-hybrid-test . & docker run -p 5000:3000 --name 11ty-app 11ty-hybrid-test"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.1.2",
    "@11ty/eleventy-img": "^6.0.4",
    "cheerio": "1.0.0-rc.10",
    "dotenv": "^16.3.1",
    "liquidjs": "^10.24.0",
    "rimraf": "^6.1.3",
    "serve": "^14.2.5"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
